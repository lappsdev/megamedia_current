<%= polaris_page(
  title: "Equipamentos",
  compact_title: true,
  back_url: "/#") do |page| %>
  <% page.with_primary_action(url: new_device_path) { "Cadastrar novo" } %>
  <%= polaris_card do %>
    <% render 'list', devices: @devices%>
  <% end %>
<% end %>
