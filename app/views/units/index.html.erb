<%= polaris_page(
  title: "Unidades",
  compact_title: true,
  back_url: "/#") do |page| %>
  <% page.with_primary_action(url: new_unit_path) { "Cadastrar novo" } %>
  <%= polaris_card do %>
    <% render 'list', units: @units%>
  <% end %>
<% end %>
