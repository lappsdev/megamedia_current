<%= polaris_page(
  title: "Usuários",
  compact_title: true,
  back_url: "/#") do |page| %>
  <% page.with_primary_action(url: new_user_path) { "Cadastrar novo" } %>
  <%= polaris_card do %>
    <% render 'list', users: @users%>
  <% end %>
<% end %>
